var game = new Phaser.Game(1500, 800, Phaser.AUTO, 'phaser');

//The variable for the main menu state
var MainMenu = function(game){};

// Using the phaser prototype to run the main menu state
MainMenu.prototype = {

	create: function()
	{
		 console.log("http://www.clovermeadowsbeef.com/amazing-facts-about-cows/");
	}
}
